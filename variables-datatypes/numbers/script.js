//

var num_obj = new Number(5); // Number constructor to use a number object
// console.log(num_obj, typeof num_obj);

var num = 100;
// tun to decimals
num = num.toFixed(2);

//num = num.toString();

// Can find number of digits in a real number this way
//num = num.toString().length;

console.log(typeof num, num);
